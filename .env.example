# Frontend environment variables
# Copy to .env for local development: cp .env.example .env
#
# ⚠️  VITE_* variables are baked into the static bundle at BUILD time.
#     For Docker, pass them as build args (see docker-compose.yml or Dockerfile).
#     For cloud platforms (Vercel, Netlify, etc.), set them in the platform's UI.

# Backend API base URL
VITE_API_URL=http://localhost:4000/api/v1

# Google OAuth client ID (must match the one configured in Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=
